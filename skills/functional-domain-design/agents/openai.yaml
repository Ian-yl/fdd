interface:
  display_name: "功能与领域设计"
  short_description: "内部运行BMAD规划，产出完整领域包与实现交接包"
  default_prompt: "Use $functional-domain-design to identify each page's interaction closures in the AI Restore release, with the architecture JSON and optional user decisions as context, then produce an approved functional domain package and implementation handoff."
